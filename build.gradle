plugins {
	id 'java-library'
	id 'maven-publish'
}

group = "${group}"
version = "${version}"

java {
	withSourcesJar()
	withJavadocJar()
}

publishing {
	publications {
		mavenJava(MavenPublication) {
			from components.java

			groupId = 'banduty.streq'
			artifactId = 'streq'
			version = "${version}"
		}
	}
}